#import "@local/bootlin:0.1.0": *

#import "/typst/local/common.typ": *

#show: bootlin-theme

#setuplabframe([U-Boot / TF-A], [
  Time to start the practical lab!

  - Communicate with the board using a serial console

  - Configure, build and install the bootloader stages:

    - _TF-A_ and _U-Boot_ on STM32MP1 and Beagleplay

    - _U-Boot SPL_ and _U-Boot_ on BeagleBoneBlack and QEMU

  - Learn _U-Boot_ commands

  - Set up _TFTP_ communication with the host

])
